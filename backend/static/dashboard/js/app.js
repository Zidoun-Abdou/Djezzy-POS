/**
 * Djezzy POS Dashboard - Main Application
 */

// Initialize on page load
document.addEventListener('DOMContentLoaded', function() {
    // Check if user has valid session (handled by Django)
    console.log('Djezzy POS Dashboard initialized');
});
